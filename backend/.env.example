# Server configuration
PORT=3000
NODE_ENV=development

# Database
MONGODB_URI=mongodb://localhost:27017/cpen321

# Owner identity returned by GET /api/name
OWNER_FIRST_NAME=Noah
OWNER_LAST_NAME=Ndambuki

# Public IP of the deployed server. Leave blank locally; set after deploying,
# since a cloud VM behind NAT cannot see its own public address.
SERVER_PUBLIC_IP=

# Course-provided pixel stream relayed to the app by Button 2
PIXEL_STREAM_URL=wss://8.229.22.124

# Google Auth 
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com

# JWT 
JWT_SECRET=your_jwt_secret_key_here

# Other API keys, secrets, and configurations